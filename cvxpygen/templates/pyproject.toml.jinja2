# Auto-generated by CVXPYgen {{ date }}.
# Content: Build configuration for scikit-build-core / CMake extension.

[build-system]
requires = ["scikit-build-core>=0.3", "pybind11>=2.8"]
build-backend = "scikit_build_core.build"

[project]
name = "cpg_module"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = ["cvxpy>=1.8", "numpy", "scipy"]

[tool.scikit-build]
cmake.source-dir = "."
cmake.build-type = "Release"
wheel.packages = []
